<?php

namespace {{namespace}}\Http\Middlewares{{sub_directories}};

use Framework\BaseMiddleware;

class {{middleware}} extends BaseMiddleware
{
	public function __invoke($request, $response, $next)
	{
		return $next($request, $response);
	}
}
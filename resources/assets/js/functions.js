/*

function readURL(input, output_el) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.readAsDataURL(input.files[0]);

        reader.onload = function(e) {
            output_el.setAttribute(e.target.result);
        };
    }
}

*/
